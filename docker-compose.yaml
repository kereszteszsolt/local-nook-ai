name: localnook

services:
  angular-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4201:4201"
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm ci && npm start -- --host 0.0.0.0 --port 4201"
    restart: unless-stopped
